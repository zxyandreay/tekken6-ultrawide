# Transient-UI Findings

**Status:** ACTIVE RESEARCH — NO ACCEPTED FINDINGS YET

Only device-validated, durable conclusions belong in this folder.

For each accepted target, record:

- visual target and intended semantic transform;
- final draw identity;
- CPU renderer/owner;
- hook address and stock instruction;
- ABI/delay-slot requirements;
- exact predicate/discriminators;
- dynamic AutoHUD parameters consumed;
- device acceptance matrix;
- resulting PRX SHA-256;
- module-space usage.

Do not move a hypothesis here merely because one test build looks correct.

Until findings are accepted, use [../HANDOFF.md](../HANDOFF.md) and [../PROBE_WORKFLOW.md](../PROBE_WORKFLOW.md).
